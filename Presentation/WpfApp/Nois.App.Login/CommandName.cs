using Nois.App.Login.Controllers;

namespace Nois.App.Login
{
    static class CommandName
    {
        public static string LoginController = typeof(LoginController).FullName;
        public static string LogoutController = typeof(LogoutController).FullName;
    }
}
